#include <stdio.h>
#include<string.h>
void main()
{
    char str[50];
    int cnt=0;

    printf("Enter string\n");
    gets(str);
    
    for(int i=0;i<strlen(str);i++)
    {
        if(str[i]==' ')
        {
            cnt++;
        }
    }
    cnt=cnt+1;
    printf("%d",cnt);
}
